# pyproject.toml
[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "puzzler-ctools"
version = "0.1"

[tool.setuptools]
ext-modules = [
  {name="puzzler.ctools", sources=["puzzler_ctools.c", "nearest_point.cxx"]}
]
